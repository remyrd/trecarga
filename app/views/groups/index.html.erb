<% provide(:title, 'All groups') %>
<h1>All groups on <%= @current_user.company_name %></h1>

<ul class="groups">
  <% @groups.each do |group| %>
    <li>
      <%= link_to group.name, group %>
    </li>
  <% end %>
</ul>